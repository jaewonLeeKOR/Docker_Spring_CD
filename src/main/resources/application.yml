spring:
  datasource:
    url: jdbc:mysql://mysql-db:3406/cicd_db?useSSL=false&allowPublicKeyRetrieval=true
    username: user
    password: password
    dirver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        jdbc:
          javax.persistence.jdbc.url